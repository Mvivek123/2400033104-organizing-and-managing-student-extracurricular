import React from 'react';
import { Box, Typography } from '@mui/material';

export default function CreateEditEvent() {
  return (
    <Box p={2}>
      <Typography variant="h4" gutterBottom>
        Create / Edit Event
      </Typography>
      <Typography>Form for admins/club leaders (stub).</Typography>
    </Box>
  );
}
import React from 'react';
import { Box, Typography } from '@mui/material';

export default function AdminDashboard() {
  return (
    <Box p={2}>
      <Typography variant="h4" gutterBottom>
        Admin Dashboard
      </Typography>
      <Typography>Overview and admin controls (stub).</Typography>
    </Box>
  );
}
import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Notifications() {
  return (
    <Box p={2}>
      <Typography variant="h4" gutterBottom>
        Notifications
      </Typography>
      <Typography>Event reminders, announcements, etc. (stub).</Typography>
    </Box>
  );
}